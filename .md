criei um arquivo peerTable.txt para permanecia dos dados do peer
criei um handler para quando o servidor for interrompido com ctrl+c ele salva os dados do peerTable.txt
tornei o socker não bloqueante para possa detectar quando é lançado um sinal de interrupção (SIGINT/SIGTERM)
self.client_socket_tcp.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
OOP
